extends:
  - recommended

apis:
  main:
    root: index.yaml
  legacy:
    root: Legacy Swagger/swagger-2.0.yml
    rules:
      operation-summary: off
      security-defined: off
      info-license: off

rules:
  operation-operationId: off
  operation-4xx-response: off

reunite:
  ignoreLint:
    "**/swagger-2.0.yml": true
    "swagger-2.0.yml": true
